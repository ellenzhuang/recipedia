// import { Link } from 'react-router-dom'

// Gets the API key from the .env.local file
const SPOONACULAR_API_KEY = process.env.REACT_APP_SPOONACULAR_API_KEY

function Recipes() {
  return (
    <div>
      <h1>Recipes</h1>
      {/* <Link to={`/${recipeID}`} className="action" /> */}
    </div>
  )
}

export default Recipes
